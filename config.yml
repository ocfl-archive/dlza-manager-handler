handler:
  host : localhost
  port : 9082
  database:
    host : "localhost"  #modify if needed
    port  : "5000"  #modify if needed
    user : "jarekp"
    password : ""  #modify if needed
    db_name : "archive_prod"
    sslmode : "require"
    schema: "management_test"
storage-handler:
  host : localhost
  port : 9083
clerk:
  host : localhost
  port : 9086

#modify if needed the hosts/ports/addresses/password, note that you can set your Password with an environnement variable called DB_PASSWORD 